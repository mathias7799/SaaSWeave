{
  "extends": "@saasweave/tsconfig/tsconfig.base.json",
  "compilerOptions": {
    "paths": {
      "@/*": ["./src/*"],

      // for shadcn CLI to resolve @saasweave/ui paths to the /packages/ui directory
      "@saasweave/ui/*": ["../../packages/ui/*"]
    },
    "types": ["node", "vite-plus/client", "mdx"],

    /* Because paraglide.js breaks types during rebuilds on dev */
    "allowJs": true
  },
  "mdx": {
    "checkMdx": true
  },
  "include": ["**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
