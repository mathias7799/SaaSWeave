/** Batch job item claim size per worker transaction. */
export const BATCH_JOB_CHUNK_SIZE = 50;

/** Lease duration for claimed batch items before recovery. */
export const BATCH_JOB_LEASE_SECONDS = 120;
